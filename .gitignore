node_modules/
dist/
client/
.env
*.log
.DS_Store
coverage/

# Lockfile sengaja tidak di-commit: CI memakai `npm install`, dan lockfile
# hasil generate di Windows bisa kehilangan binary rollup/esbuild versi Linux
# sehingga build image gagal.
package-lock.json
